<body>
    <h1>${Resource.msgf('message.hello', 'newsletter', null, pdict.firstName, pdict.lastName)}</h1>
    </br>
    <h2>${Resource.msg('message.avaliable.mail', 'newsletter', null)}</h2>
    </br>
    <p>${pdict.couponCode}</p>
</body>